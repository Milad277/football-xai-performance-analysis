# Data

This project is based on open-access football event data from the Wyscout Open Data repository.

The raw dataset is not included in this repository due to size and licensing considerations.

Expected structure:

```text
data/
├── raw/
│   └── Wyscout JSON files
└── processed/
    └── Cleaned and processed datasets
